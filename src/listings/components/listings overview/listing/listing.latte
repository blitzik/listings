
{var $listingName = $listing->getName() ?? 'Bez názvu'}

<a href="{plink :Listings:Member:ListingDetail:default id => $listing->getId()}" class="listing" title="{$listingName}">
    <div class="row">
        <div class="col-xs-4">
            <b>{$listing->getMonth()|monthByNumber}</b> {$listing->getYear()}
        </div>
        <div class="col-xs-8 text-xs-right">
            <small>{$listingName}</small>
        </div>
    </div>
</a>